import React from 'react';
import NearbyStations from './nearbyStations';
import {shallow} from 'enzyme';

it('should render nearby stations container', () => {
    const wrapper= shallow(<NearbyStations />);

});
